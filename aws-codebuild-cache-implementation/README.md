# AWS CodeBuild Cache Implementation

Example `buildspec.yaml` for an Android build pipeline using AWS CodeBuild, Gradle cache, and Firebase App Distribution.

The parameter names are examples only. Real values should be stored in AWS Systems Manager Parameter Store or AWS Secrets Manager.
